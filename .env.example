# Copy to .env and update as needed

# FastAPI/JWT
SECRET_KEY=change-me-in-dev
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_DAYS=7

# Database
DB_USER=devuser
DB_PASSWORD=devuser
DB_HOST=127.0.0.1
DB_PORT=3306
# Match your imported seed DB name
DB_NAME=prep_iq3

# Optional CORS origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000
